Run the sprint gate first. If PASS: commit all changes with a descriptive message, push to the current branch, and monitor the CI/CD pipeline. Vercel will auto-deploy the frontend and Coolify will auto-deploy the backend to Hetzner. After 60 seconds, run a health check against https://api.qanun.io/health. Report the deployment status for both frontend and backend. If FAIL: stop and report what needs fixing.
